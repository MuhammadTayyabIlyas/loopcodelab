# saas/

Auth and session helpers. This build runs single-user, so these modules are
lightweight helpers that keep the rest of the server importing cleanly; access
control is handled in front (for example nginx auth).
